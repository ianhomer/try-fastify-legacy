{
  "name": "@try-fastify/eslint-config",
  "version": "0.0.1",
  "description": "Linting configuration",
  "main": "index.js",
  "scripts": {
    "lint": "npx eslint --ext js .",
    "lint:fix": "npx eslint --ext js . --fix"
  }
}
