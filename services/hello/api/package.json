{
  "name": "@try-fastify/hello-api",
  "version": "0.0.1",
  "description": "",
  "main": "src/index.js",
  "scripts": {
    "build": "tsc",
    "lint": "npx eslint src --ext .ts",
    "lint:fix": "npx eslint src --ext .ts --fix",
    "show:tsconfig": "tsc --showConfig",
    "start": "ts-node-dev src/index",
    "serve": "node dist/index",
    "test": "jest"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@try-fastify/foo": "^0.0.1",
    "fastify": "^3.6.0",
    "fastify-cli": "^2.3.0",
    "fastify-healthcheck": "^3.0.0"
  },
  "devDependencies": {
    "@try-fastify/eslint-config": "^0.0.1",
    "@try-fastify/prettier-config": "^0.0.1",
    "@try-fastify/tsconfig": "^0.0.1"
  }
}
